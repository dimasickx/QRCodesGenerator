using System.Collections.Generic;
using System.Linq;

namespace QRCodesGenerator
{
    public static class DataReadyToPrint
    {
        public static string CombinationBlocks(List<DataBlock> dataBlocks, List<CorrectionBlock> correctionBlocks)
        {
            // var result = new int[dataBlocks.Count * 2];
            // for (int i = 0; i < dataBlocks.Count; i++)
            // {
            //     for (int j = 0; j < dataBlocks[i].Data.Count; j++)
            //     {
            //         result[] = dataBlocks[i].Data[j]; 
            //     }
            // }
            return "";
        }
    }
}